export type ThemeType = typeof theme;

export const theme = {
  primary: '#000',
  text: '#000',
  background: '#fff',
  pokemon: {
    type: '#ad2a40',
    details: {
      background: '#000',
      title: '#fff',
      data: '#f6f588'
    },
    image: {
      background: '#000'
    }
  }
};

export default theme;
